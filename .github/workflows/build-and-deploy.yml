name: Build and Deploy
run-name: ${{ github.actor }}
on: [push]
jobs:
  build:
    runs-on: self-hosted
    strategy:
      matrix:
        node-version: [22.x]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: node -v
